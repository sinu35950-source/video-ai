# Prompt → Video Generator (Next.js + Replicate)

## What this is
A simple web app that takes a text prompt and generates a video using a Replicate-hosted text-to-video model.

## Requirements
- Node.js 18+
- A Replicate account + API token
- A Replicate text-to-video model Version ID

## Setup
1) Install deps
```bash
npm install
